import { describe, it, expect } from 'vitest';
import StrongholdsApp from '../src/apps/StrongholdsApp.svelte';

describe('StrongholdsApp', () => {
  it('is importable', () => {
    expect(StrongholdsApp).toBeTruthy();
  });
});

